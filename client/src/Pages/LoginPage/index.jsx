import React from 'react';
import { LoginCard } from '../../Components/export';

const LoginPage = () => {
    return (
        <div className="login-page">
            <LoginCard />
        </div>
    );
};

export default LoginPage;
